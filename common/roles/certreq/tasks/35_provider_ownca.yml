- name: Common tasks
  ansible.builtin.include_tasks:
    file: "30_provider_common.yml"
  ignore_errors: true

- name: Create crt and sign {{ certreq_csr_cn }}
  delegate_to: "{{ certreq_ownca_host }}"
  community.crypto.x509_certificate:
    path: "{{ certreq_ownca_root_dir }}/pki/crt/{{ certreq_csr_cn }}.crt"
    csr_content: "{{ certreq_csr_result.csr }}"
    ownca_path: "{{ certreq_ownca_cacert }}"
    ownca_privatekey_path: "{{ certreq_ownca_cakey }}"
    provider: ownca
    return_content: true
    backup: true
  register: certreq_crt_result

- name: save crt {{ certreq_csr_cn }}
  ansible.builtin.copy:
    dest: "{{ certreq_crt_file }}"
    content: "{{ certreq_crt_result.certificate }}"
    owner: "{{ certreq_default_owner }}"
    group: "{{ certreq_default_group }}"
    mode: "{{ certreq_default_file_mode }}"
    backup: true
  notify: "{{ certreq_notify }}"
